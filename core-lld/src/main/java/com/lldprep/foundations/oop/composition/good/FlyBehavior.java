// COMPOSITION GOOD: Focused interface for flying behaviour.
// NoFly is a valid implementation (null object pattern) — no stubs, no exceptions.
package com.lldprep.foundations.oop.composition.good;

public interface FlyBehavior {
    void fly();
}
